# {{ MODULE_NAME }}
Autogenerated example repository for installation with Pip or Conda.

## Post Creation Build Steps
Steps to perform after generation of this repository:
1. Create your Python files or copy existing files into `src/{{ MODULE_NAME }}`
1. Update `src/{{ MODULE_NAME }}/__init__.py` to import publicly-exposed classes from your Python files.
   - Example: If you have classes in a file `src/{{ MODULE_NAME }}/test.py` your line in `src/{{ MODULE_NAME }}/__init__.py` would be:
     `from .test import *`

## Building Pip Installable
You can build this repository to be installed with Pip with the following commands:
```
# Create a virtual environment
python -m venv venv
source venv/bin/activate

# Install the necessary libraries to build and upload the package
pip install -r requirements.txt

# Build the package
rm -rf dist/
python -m build

# You can pip install your package using:
pip install dist/*.whl
```

## Building Conda Installable
You can build this repository to be installed with Conda with the following commands:
```
# Install conda-build
conda install -y conda-build

# Build the package
conda-build --override-channels -c conda-forge --output-folder local-channel recipe

# You should have your project's .tar.bz2 file in local-channel/noarch/
ls -lah local-channel/noarch/

# You can install your project in a Conda environment with:
conda install -y --override-channels -c conda-forge -c /landing/local-channel/ {{ MODULE_NAME }}
```
